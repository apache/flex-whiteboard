1. build FalconJS (see README)
2. build JS framework into apacheFlex.js (Closure Compiler)


ANT script

read AS project location
read AS project name
read JS framework location
read FalconJS location

create tmp directory
copy all as files from AS project to temp

compile all AS files to JS files

run JS files through Closure

set 

index.html.template: ${appName} must be replace by name of the 'main' AS class/file
